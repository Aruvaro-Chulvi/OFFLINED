FFMPEG — SOURCE CODE NOTICE / AVISO DE CÓDIGO FUENTE
===========================================================

[EN]
The file `ffmpeg-win-x86_64-v7.1.exe` (bundled via the Python package
`imageio_ffmpeg`, used for audio format conversion in the Notes and
Voice Generation sections) is an UNMODIFIED third-party build of FFmpeg,
compiled by gyan.dev ("7.1-essentials" build). The full license text is
available at:
  /licenses/LICENSE_FFmpeg_GPL-3.0.txt

This build was compiled with `--enable-gpl --enable-version3` (includes
libx264/libx265/libxvid and other GPL-licensed components), so the
binary as a whole is licensed under the GNU General Public License
v3.0 (GPL-3.0), not LGPL. It is NOT covered by the Offlined custom
license. You may use, copy, modify and redistribute it under the terms
of the GPL-3.0.

Offlined invokes this binary exclusively as a SEPARATE PROCESS via
`subprocess.run(...)` (backend/main.py, audio-conversion helpers) —
the same "mere aggregation" pattern already used for kiwix-serve,
Notepad++ and readpst (see ATTRIBUTION.md). The GPL copyleft does not
extend to Offlined's own proprietary code.

Exact version (output of `ffmpeg -version`):
  ffmpeg version 7.1-essentials_build-www.gyan.dev

Corresponding source code (GPLv3, section 6):
  - Official FFmpeg source (matching upstream release 7.1):
      https://ffmpeg.org/download.html
      https://github.com/FFmpeg/FFmpeg/archive/refs/tags/n7.1.tar.gz
  - Build scripts / patches used for this specific Windows build
    ("gyan.dev essentials"):
      https://www.gyan.dev/ffmpeg/builds/
  - Python packaging layer that downloads/exposes this binary:
      https://github.com/imageio/imageio-ffmpeg

[ES]
El archivo `ffmpeg-win-x86_64-v7.1.exe` (incluido a través del paquete
Python `imageio_ffmpeg`, usado para convertir formatos de audio en las
secciones de Notas y Generador de voz) es un binario de terceros SIN
MODIFICAR de FFmpeg, compilado por gyan.dev (build "7.1-essentials").
El texto completo de la licencia está en:
  /licenses/LICENSE_FFmpeg_GPL-3.0.txt

Este build se compiló con `--enable-gpl --enable-version3` (incluye
libx264/libx265/libxvid y otros componentes bajo GPL), por lo que el
binario en su conjunto está licenciado bajo GPL-3.0, no LGPL. NO está
cubierto por la licencia personalizada de Offlined.

Offlined lo invoca EXCLUSIVAMENTE como proceso separado vía
`subprocess.run(...)` (backend/main.py, helpers de conversión de audio)
— el mismo patrón de "mera agregación" ya usado con kiwix-serve,
Notepad++ y readpst (ver ATTRIBUTION.md). El copyleft de GPL no se
extiende al código propietario de Offlined.

El código fuente correspondiente (GPLv3, sección 6) está disponible en
los enlaces indicados arriba.
